const btnStart = document.querySelector('.my-btn-start');
const btnStop = document.querySelector('.my-btn-stop');
const board = document.querySelector('.container-board')
const myApplication = new Application(btnStart,btnStop,board);
